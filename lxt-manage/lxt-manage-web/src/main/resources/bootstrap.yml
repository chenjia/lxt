management:
  endpoints:
    web:
      exposure: 
        include: "*"
      cors:
        allowed-origins: "*"
        allowed-methods: "*"
spring:
  cloud:
    config:
     profile: dev
     label: master
     uri: http://localhost:8900/lxt-config