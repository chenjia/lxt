eureka:
  client:
    registryFetchIntervalSeconds: 30
    serviceUrl:
      defaultZone: http://localhost:8100/lxt-eureka/eureka/,http://localhost:8200/lxt-eureka/eureka/

server:
  port: 8900
  servlet:
    contextPath: /lxt-config

spring:
  application:
    name: lxt-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ms/lxt-config
          searchPaths: config
          username: 251746034@qq.com
          password: